const Util = {
  r: require('ramda'),
  bn: require('bn.js'),
  buf: require('./buf'),
  Writer: require('./writer'),
  Reader: require('./reader'),
  Parser: require('./parser')
};

module.exports = Util;
